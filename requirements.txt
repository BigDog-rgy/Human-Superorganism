# LLM Council Dependencies

# Core API clients
# Upper bound is deliberate: anthropic 1.x removed `temperature` from
# Messages.create(), which every council/briefing script passes. Unpinning this
# broke the weekly Action after 2026-08-19. See the temperature note in README
# before lifting it.
anthropic>=0.80.0,<1.0
openai>=1.54.0
google-genai>=0.3.0

# Utilities
python-dotenv>=1.0.0

# Visualization
pyvis>=0.3.2

# Optional: for enhanced output formatting
rich>=13.9.0
